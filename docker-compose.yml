version: '3.9'

services:
  api:
    image: ghcr.io/0-5788719150923125/src:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./index.js:/src/index.js
    environment:
      WEBUI: enabled
      ANONYMOUS: 'true'
    ports:
      - 9668:9666
